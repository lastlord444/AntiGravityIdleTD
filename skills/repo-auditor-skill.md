# Repo Auditor Skill

- **Goal:** Maintain repository health.
- **Tasks:**
  - Run `git status` and `git diff` before any major changes.
  - Ensure no uncommitted work is lost.
  - Verify branch hygiene.
